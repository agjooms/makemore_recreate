# Makemore Project - Recreating Andrej Karpathy's Tutorial

git commands:
- git add 'filename' -> Stages Changes
- git commit -m "message" -> Committing
- git push -u origin main -> Push commits (I just use VSCode so I don't need token)